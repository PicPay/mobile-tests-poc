# Default profile
default: -p json -p android

# Saucelabs profile
ios: -p json PLATFORM=ios
android: -p json PLATFORM=android
json: -f json -o log/report.json -f pretty
# ios:     -t @ui ENV=sauce PLATFORM=ios
# android: -t @ui ENV=sauce PLATFORM=android